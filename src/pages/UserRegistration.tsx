import React from 'react';
import { ContentBox, Wrapper } from '../components';

const UserRegistration: React.FC = () => {
  return (
    <Wrapper>
      <ContentBox>Usuário</ContentBox>
    </Wrapper>
  );
};

export default UserRegistration;
